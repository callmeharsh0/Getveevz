// useReducedMotion — NOT YET IMPLEMENTED
// See ARCHITECTURE.md section 6. Purpose: read the OS-level
// prefers-reduced-motion media query so animation/easing can be reduced.
export default function useReducedMotion() {
  return false; // placeholder default
}
